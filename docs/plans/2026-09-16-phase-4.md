# Phase 4 — the learning loop

Status: started Sep 16, 2026 (evening), branch `phase-4/1-capture` (worktree `OpenMausBot-phase3`),
stacked on `phase-3/5-bakeoff`. Fifth phase of the harness programme
(`2026-09-15-harness-roadmap.md`, "Phase 4"). Built in measured parts with a first-run pass each
(AGENTS.md rule 7).

Standing rules: every item works on every engine (full / degraded / not supported, stated);
T1–T10, the recall set and the Phase 3 live passes stay green; PRs stacked and small; every
overlapping PR named with a verdict; the user's first run is walked before hand-over.

| Part | Scope | Spec | Metric | Status |
| --- | --- | --- | --- | --- |
| 1 · fact capture | at the end of an attended direct turn, one harness call (fingerprinted, budget-checked, booked) reads the person's words and the bot's words separately and proposes candidates in a fixed shape; candidates under 0.4 confidence dropped, at most 8 per turn, deduped against the notebook, appended ADD-only with the `auto` marker and journaled; buffered per thread and flushed after a quiet spell or ten turns, never on the turn path; unattended turns and rooms skipped; opt-in per bot | `docs/superpowers/specs/2026-09-16-phase-4-capture-design.md` | capture set (tell a fact without asking to remember, archive the thread, ask in a new one): main 0/N → branch N/N; a small-talk thread yields ≤2 captures over ten turns; every capture carries date, source, `auto`; T1–T10 and the recall set unchanged | built on `phase-4/1-capture`: `server/capture.ts`, `bufferCapture` / `runCapture` in the server, `memoryCapture` on the bot, `memory.captureQuietMs`; unit + e2e green |
| 2 · freshness and consolidation | `· confirmed YYYY-MM-DD ·` on entries the bot or person restated; a consolidation pass (collect → dedupe → consolidate → apply with hash check → journal) that merges duplicates deterministically, supersedes contradictions found by one strict-JSON call, archives untouched lines past an age, snapshots first, and never removes more than a fifth of the file in one pass; runs nightly or on demand | `docs/superpowers/specs/2026-09-16-phase-4-consolidate-design.md` | a seeded notebook with duplicates and contradictions: duplicates merged, contradictions struck with a trace, nothing else removed, the floor respected; the journal shows one entry per pass | built on `phase-4/2-consolidate`: `server/consolidate.ts`, `consolidateMemory` + route + nightly timer, confirmed stamps from capture, capture writes journaled |
| 3 · skills that learn | an import lock (source repository, path, content hash) for every imported skill beside `syncSkillLinks`' hash check; post-task reflection: a board task or routine run judged complete with tool steps drafts one candidate skill (when to use / procedure / pitfalls / verification) into the existing `/learn` review card, never enabled without a person; usage counters on learned skills | `docs/superpowers/specs/2026-09-16-phase-4-reflection-design.md` | e2e: a judged-complete task with tool steps stages exactly one candidate, a repeat stages none; the import lock already flags a changed hash (existing code) | built on `phase-4/3-reflection`: `server/reflection.ts`, `reflectOnTask` after a complete verdict, `learn.reflect`; usage counters deferred (no signal until file reads are brokered) |
| 4 · routine notes | a bot's "note for the next run" carried by continuity; an "already scheduled" refusal when a proposal duplicates an existing routine | `docs/superpowers/specs/2026-09-16-phase-4-routine-notes-design.md` | routines suite | built on `phase-4/4-routine-notes`: `extractNextRunNote`, `RoutineRun.nextNote`, `<previous-run-note>` in the carry, the twin refusal in `create()` for proposals |

Deferred out of Phase 4 with reasons: `run_code → save_skill → run_skill` with sandboxing
(Phase 5's sandbox interface first); the offline mini-benchmark before promotion (needs part 3's
counters to have data); HAL submission and the auto-research loop (a runner box).

## PRs checked before building (AGENTS.md rule 2)

- **#1040 (merged): memory entries carry a date and a source, refuse to overflow, supersede
  leaves a trace.** Part 1 writes through the same grammar and the same `updateMemory`, adding
  one marker (`auto`); part 2 uses `supersede` for contradictions. Taken as the base.
- **#594 / #656 / #1066 (merged): opt-in `/learn`, review-gated learned skills, Save as skill.**
  Part 3's reflection stages through this exact card; nothing is enabled without the review.
- **#952 (open, external): per-bot Hindsight memory** — an external memory service with
  asynchronous extraction after the final reply. Verdict: not taken (memory stays local plain
  markdown, decision in the gap analysis); its lessons kept: submit extraction after the final
  reply, off the turn path, per thread, and report acceptance apart from completion.
- **#1080 (closed, conflicting): rolling task notebook + host recycle.** Superseded by Phase 1's
  compaction; its "compile a notebook from every settled turn" is what main's daily log line
  (#1302) and Phase 0's digest already do.
- **#1302 (merged on main): recent-work brief, daily log line, recall by time.** Reconciled at
  the chain's re-stack; part 1's captures are facts, not activity, so the two do not overlap.
- **#570 (open): app-wide skills catalog.** Not this phase; part 3's lock records where a skill
  came from, which a catalog would read.
- **#903 (open, this account): team memory.** Phase 6.

## Main has moved

47 commits since the chain's base; the re-stack before merge is still pending (needs force-push
permission). Nothing in the 22 newest commits touches memory or skills beyond `skill-fetch.ts`
(#1344, a GitHub blob link import), noted for part 3.

## Why this order

Capture first because everything else consolidates or promotes what capture wrote. Freshness
second because auto-captured facts are the first ones that go stale. Skills third because the
reflection needs Phase 3's verdicts to know a task was done right. Routine notes last, smallest.

## Findings (Phase 4)

- **F24 — a twin proposal still raised a card.** The refusal lived in `create()`, which runs on
  confirmation; the bot's proposal names its text `instructions`, so a naive check before the card
  saw no prompt. Fixed on `phase-4/4-routine-notes`: the proposal is sanitised the way the card
  would sanitise it and checked before any card.
- **F25 — the model missed a contradiction beside its duplicate.** Fixed on
  `phase-4/2-consolidate`: the contradiction call is asked over the deduplicated list.
- **F26 — a trivial task is not a skill.** The reflector answered NONE for "write hello to a file",
  twice, and drafted a real skill for a release-checklist procedure. Kept as the intended
  discipline; the prompt says so.
- **Trap, again:** a TypeScript parameter property in `CaptureBuffer` kept the server from booting
  in strip-only mode; the memory note about it now has a second data point.
