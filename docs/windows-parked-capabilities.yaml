# Safe bootstrap state for the interactive Windows CUA Scheduled Task.
# The Windows companion replaces this file atomically with a short-lived,
# approved task capability before OpenMausBot mounts the CUA MCP bridge.
version: 3
expires_after: 8760h
idle_timeout: 20m

allow:
  tools: []

resources:
  desktop:
    display: false
