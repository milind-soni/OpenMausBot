# Live check — Phase 4 on a standalone instance (Sep 16, 2026)

Rule 7 first-run passes: standalone harness on a throwaway folder, one fresh bot with only the
part's own switch turned on, live Claude Code.

## Part 1 — fact capture (`phase-4/1-capture`@c836acf1)

Setup: fresh bot, `memoryCapture: true`, `memory.captureQuietMs: 8000`. Three turns in the
person's own chat with two facts said in passing and never a "remember this", then the chat
archived and a fresh thread asked. Script: the conversation's `first-run-p4a.ts`.

| Step | Result | What was seen |
| --- | --- | --- |
| A. "Quick one before we start: our release codename this quarter is blue-falcon-42. Anyway, what's a good name for a golden retriever?" then "We ship on Fridays by the way, never on Mondays…" then "Thanks! Talk later." | — | ordinary replies (dog names) |
| B. After the quiet spell, the notebook | pass | `- 2026-09-16 · from chat "…", captured · This quarter's release codename is blue-falcon-42.` and `… · Omkar's team ships on Fridays and never on Mondays.` |
| C. Nothing else kept | pass | no line about dog names, the question, or "talk later" |
| D. Chat archived, fresh thread: "What is our release codename, and which day do we ship?" | pass | *"Release codename is blue-falcon-42, and your team ships on Fridays."* — from the notebook, since recall cannot read an archived thread |
| E. Metrics | pass | turns 4, harness calls 2 (one per speaker; the bot's words yielded nothing) |

Kept 2 of 2 candidates from 3 turns. Not tried live: the ten-turn flush, a budget refusal
mid-flush, an engine without a one-shot (unit / e2e).

## Part 2 — freshness and consolidation (`phase-4/2-consolidate`@d8df2870)

Setup: fresh bot, a seeded notebook of eleven dated lines: an exact duplicate ("The team ships
on Fridays" twice), a correction ("switched to shipping on Tuesdays"), a stale importance-1 line
from 2025, and seven current lines. Two passes on demand. Script: `first-run-p4b.ts`.

| Step | Result | What was seen |
| --- | --- | --- |
| A. First pass | pass | duplicate removed, contradiction struck, floor reached (11 entries → 2 changes) |
| B. The struck line | pass | `- 2026-09-10 · from chat "b", captured · ~~The team ships on Fridays.~~ · superseded 2026-09-16`; the Tuesdays line kept |
| C, D. Nothing else touched | pass | moonbase, Priya, standup, pnpm, tags, Pune, "short replies" all intact |
| E, F. Second pass | pass | the 2025 plant line moved to `memory/archive.md` with `· archived 2026-09-16` |
| G. Journal | pass | two entries `via: consolidate` |
| H. Cost | — | two harness calls |

The first run of this pass (before `d8df2870`) missed the contradiction on the first pass and
caught it on the second: the model was shown the duplicate pair beside its correction. The
contradiction call is now asked over the deduplicated list; the rerun above caught it first time.

## Part 3 — reflection into a candidate skill (`phase-4/3-reflection`@b909d47d)

Setup: fresh bot on Auto with an empty project folder; a board task with a real procedure
("set up a release checklist script: version field, changelog heading, no TODO-BEFORE-RELEASE
markers; wire it as npm test; make it pass"). Script: `first-run-p4c.ts`.

| Step | Result | What was seen |
| --- | --- | --- |
| A. Judged | pass | `Verified: complete (confidence 0.95)`, `Gates: test pass` |
| B. Candidate staged | pass | `release-checklist-setup` — *"Create and wire a release validation script that enforces version consistency, changelog presence, and pre-release marker cleanup"*, source `reflection on task "Release checklist tool"` |
| C. Review card in the run thread | pass | the SKILL.md preview: When to use (three cases), Procedure (four numbered steps with the exact checks and exclusions), Pitfalls, Verification |
| D. The same task again | pass | no second candidate: the model answered NONE this time, and the name would have been refused anyway |
| E. Cost | — | two turns, four harness calls (two verdicts, two reflections) |

The first run of this pass used a trivial task (write hello to a file, run the test); the model
answered NONE both times — the right call for a one-off, and the reason the prompt says "a
procedure someone would want to repeat, not a one-off answer".
