# Live check — Phase 2 on a standalone instance (Sep 16, 2026, morning)

Standalone harness from `phase-2/tools`@7b04eca2 on its own data folder (`features.board`, `tools.deferred`,
`board.defaultBudgetUsd: 1`, `recall.captures: false`), live Claude Code, Codex for T4. Scripted through the
same routes the app uses (the packaged app refuses scripted turns by design).

| Test | Result | What was seen |
| --- | --- | --- |
| Board task runs on the dispatcher and books its cost | pass | a task capped at $0.20 ran on the next tick, `spentUsd` $0.137 after turn 1 and $0.192 after turn 2, status `review`, result `[digest] · no tool calls · reply: hello` |
| Warn at 70%, pause at the cap, resume on a raised cap | **pass** | cap $0.08: turn 1 $0.055 (69%, so no warning yet — one point under the line), turn 2 $0.111 → `blocked`, reason *paused, needs a budget increase*, the comment says "$0.111 of $0.080 spent"; a cap below the spend refused with 400; raised to $5 → `ready` |
| Engine fallback | configured only | the alternate is accepted and a bogus one refused; a real quota failure cannot be forced on a live account, so the switch itself stands on `fallback.e2e.test.ts` |
| Deferred tools | **pass** | asked "which rooms exist, use your tools": Claude called `search_tools`, then `use_tool` → `list_rooms`, and answered with the room's name |
| Mention tokens in a routine | **pass** | the run's first turn read "Tell me the name of Worker (bot b480df67…)" with the resolved references block; the routine's stored instructions keep the token; the run completed with the worker's name |
| T1–T4 | all correct | with deferred tools on: T2 turn 1 input **33,968** against 43,634 on main (−22%); T1 68,556 against 131,895; T3 69,036 against 88,670 |

## The number worth knowing

Deferred tool loading takes about 10,000 input tokens off every Claude call on a bot with the agents tools
mounted: the tool schemas the proxy advertises were costing more than the harness's own prompt. It is off
by default in this PR because it adds one `search_tools` step when a rare tool is needed; the live run shows
Claude taking that step correctly.
