abi <abi/4.0>,
include <tunables/global>

# Only the packaged, root-owned executable can create its browser sandbox.
# Never widen this to a home directory or the portable AppImage mount path.
profile astra-browser /opt/Astra/resources/browser-engine/chrome/chrome-headless-shell-linux64/chrome-headless-shell flags=(unconfined) {
  userns,
}
